# Lego Builder (static)

Simple browser-based Lego-ish brick builder.

- Static HTML/JS (no build step)
- Drag bricks around a grid
- Saves to `localStorage`

## Local test
Open `index.html` directly, or serve it:

```bash
npx serve apps/lego
```
