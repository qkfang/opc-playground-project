# Airplane (static)

Simple airplane-themed website sub-app.

- Static HTML/CSS/JS (no build step)
- Responsive landing page with About, Fleet, Destinations, and Safety sections
- Interactive pre-flight safety checklist

## Local test
Open `index.html` directly, or serve it:

```bash
npx serve apps/airplane
```
